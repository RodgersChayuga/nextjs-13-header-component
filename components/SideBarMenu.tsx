'use client';

import React from 'react';
import SideBarMenuItems from './SideBarMenuItems';

const SideBarMenu = () => {
  return (
    <div>
      <SideBarMenuItems />
    </div>
  );
};

export default SideBarMenu;
