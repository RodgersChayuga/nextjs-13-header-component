import React from 'react';

import { Logo, SearchBar, Profile } from '../components';

const TopNavBar = () => {
  return (
    <div>
      <Logo />
      <SearchBar />
      <Profile />
    </div>
  );
};

export default TopNavBar;
