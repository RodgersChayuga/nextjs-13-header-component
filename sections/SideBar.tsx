import React from 'react';

import { SideBarMenu, ThemeToggleMode } from '../components';

const SideBar = () => {
  return (
    <div>
      <SideBarMenu />
      <ThemeToggleMode />
    </div>
  );
};

export default SideBar;
